#include <algorithm>
#include <iostream>
#include <string>
#include <vector>

int main() {
    std::cout << std::fixed;
    long double a = 0;
    long double b= 0;
    
    std::vector<long double> e = { - 1};
    std::vector<long double> f = { 1 , -1, 4, (-!-!(-a + b)) + (-e[0]) };
    
    bool g   ;
    long double h ;
    std::vector<std::string> i;
    std::vector<bool> j = {};
    
    std::vector<std::string> k = i;
    
    std::string l = std::string("4");
    std::string m;
    std::string o = std::string(m) + std::string(l);
    std::string p = (std::string(m) + std::string(l) + std::string(o)) + (std::string(m)) + (std::string(l) + (std::string(m) + ((((std::string(l)))))));
    
    char q = 'a';
    char r = 'b';
    char s = q - r;
    
    long double t = 1 +-+! 4;
    long double u = t * - + - -+!q;
    return 0;
}