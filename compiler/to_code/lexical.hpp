#include <iostream>
#include <fstream>
#include <string>
#include <sstream>
#include <map>
#include <vector>

#include "token.hpp"

struct Lexical {
    std::vector<Token> tokens;

    void tokenizeLine(std::string& str, int numLine) {
        
    }

    void tokenizeBitmap(std::string& path) {
        
    }
};